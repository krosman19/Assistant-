import {Nav} from '@/components/Nav';
export default function Setup(){return <main className="shell"><Nav/><section className="hero"><h1>Setup wizard</h1><p className="muted">Connect the underlying accounts behind Apple Mail, Apple Calendar, Gmail, Outlook, IMAP, and CalDAV.</p></section><div className="grid">{['Create user','Choose briefing time, tone, length','Connect Gmail or Microsoft','Add IMAP mail including iCloud Mail','Add CalDAV including iCloud Calendar','Generate first audio briefing'].map((x,i)=><div className="card span6" key={x}><span className="pill">Step {i+1}</span><h2>{x}</h2><p className="muted">Demo mode lets you continue immediately without credentials.</p></div>)}<div className="card"><h2>Apple Mail limitation</h2><p>Apple Mail itself does not generally expose a cloud API for third-party web apps. Daily Briefing Agent uses provider-level integrations instead: Gmail API, Microsoft Graph, IMAP, and CalDAV. For iCloud, use an Apple app-specific password with CalDAV at https://caldav.icloud.com and IMAP at imap.mail.me.com.</p></div></div></main>}
