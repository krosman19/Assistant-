import {Nav} from '@/components/Nav';import {generateDemoBriefing} from '@/lib/briefing';
export default async function Archive(){const b=await generateDemoBriefing();return <main className="shell"><Nav/><section className="hero"><h1>Briefing archive</h1><input placeholder="Search transcripts, actions, citations..."/></section><div className="card"><h2>{b.title}</h2><p className="muted">Today · {Math.round(b.duration_seconds/60)} min</p><audio controls src={b.audio_url}/><details><summary>Transcript and citations</summary><p style={{whiteSpace:'pre-line'}}>{b.transcript}</p><pre>{JSON.stringify(b.citations_json,null,2)}</pre></details></div></main>}
