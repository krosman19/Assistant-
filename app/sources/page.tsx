import {Nav} from '@/components/Nav';import {iCloudGuide} from '@/lib/integrations';
const sources=['Gmail OAuth','Outlook / Microsoft Graph','IMAP mail','CalDAV calendar','iCloud Mail','iCloud Calendar'];
export default function Sources(){return <main className="shell"><Nav/><section className="hero"><h1>Sources & integrations</h1><p className="muted">Not limited to Google. Connect provider-level accounts used by Apple Mail and Apple Calendar.</p></section><div className="grid"><div className="card span7"><h2>Connect accounts</h2>{sources.map((s,i)=><div className="item" key={s}><b>{s}</b><p className="muted">{i<2?'OAuth read-only scopes for mail and calendar.':'Read-only setup with encrypted app-specific credentials.'}</p><button className="btn">Connect / simulate</button> <span className="status">demo ready</span></div>)}</div><div className="card span5"><h2>Apple / iCloud setup</h2><p className="warn">Direct Apple Mail app access is not available to web apps.</p><p>{iCloudGuide.appleMailNote}</p><div className="item"><b>iCloud Calendar CalDAV</b><p>{iCloudGuide.calendarServer}</p></div><div className="item"><b>iCloud Mail IMAP</b><p>{iCloudGuide.imapServer} with SSL/TLS and app-specific password.</p></div><p className="muted small">Credentials are encrypted at rest and can be disconnected per source.</p></div></div></main>}
